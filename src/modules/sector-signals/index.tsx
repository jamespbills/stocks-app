import type { ReactElement } from 'react'

export default function SectorSignals(): ReactElement {
  return (
    <div
      className="flex h-full items-center justify-center"
      style={{ color: 'var(--color-text-muted)' }}
    >
      Sector Signals — coming in Phase 2
    </div>
  )
}
